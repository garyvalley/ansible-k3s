---
packages:
        - kubelet
        - kubeadm
        - kubectl

gpgkey: "https://packages.cloud.google.com/apt/doc/apt-key.gpg"

repo: "deb https://apt.kubernetes.io kubernetes-xenial main"

node_ip: "{{ hostvars['master']['ansible_host'] }}"

apiserver: "192.168.50.10"

